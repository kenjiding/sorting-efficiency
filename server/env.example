# 后端服务器环境变量
# 复制此文件为 server/.env 并修改配置

# 服务器端口
PORT=7890

# MongoDB 连接字符串
# 本地开发（使用 Docker Compose 中的 MongoDB）
MONGODB_URI=mongodb://localhost:27017/sorting-management

# 如果在 Docker 容器中运行后端，使用：
# MONGODB_URI=mongodb://mongodb:27017/sorting-management

# 生产环境 MongoDB（如果有）
# MONGODB_URI=mongodb+srv://user:password@cluster.mongodb.net/sorting-management
# MONGODB_TLS=true
# MONGODB_TLS_ALLOW_INVALID=false

# 环境
NODE_ENV=development

